"""NiceGUI frontend for PyTeslaCoil.

This package contains all UI code. It depends on the engine
(:mod:`pyteslacoil`) but the engine never imports from here. Run with
``pyteslacoil`` (entry point) or ``python -m ui.main``.
"""
