"""Pure-Python physics engine for PyTeslaCoil.

This package contains the calculation routines and **must not import any UI
code**. The engine is intentionally a standalone library so it can be reused
from notebooks, web APIs, or external tooling.
"""
